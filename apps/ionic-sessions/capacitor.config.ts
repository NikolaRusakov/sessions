import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'ionic-sessions',
  webDir: '../../dist/apps/ionic-sessions',
  bundledWebRuntime: false,
};

export default config;
